// Placeholder smoke test — verifies the app widget compiles and mounts.

import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test placeholder', (WidgetTester tester) async {
    // Intentionally empty — replace with real widget tests as the app grows.
    expect(true, isTrue);
  });
}
